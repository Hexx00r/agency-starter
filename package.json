{
  "name": "agency-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable static site starter for trade-service clients. Edit site.config.ts, push to main, get a live site.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
